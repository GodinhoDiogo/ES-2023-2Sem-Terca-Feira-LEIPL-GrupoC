/**

The Main class is the entry point of the program.
It creates an instance of the ChooseImport class to start the import process.
The ChooseImport class is responsible for displaying the menu and handling user input.
*/
import menus.ChooseImport;

public class MainTest {
	/**
	 * The main method creates an instance of the ChooseImport class to start the import process.
	 *
	 * @param args the command line arguments
	 */
	public static void main(String[] args) {
	    new ChooseImport();
	}
}
